<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
"http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">
<refentry>
  <refmeta>
    <refentrytitle>wdrender</refentrytitle>

    <manvolnum>1</manvolnum>
  </refmeta>

  <refnamediv>
    <refname>wdrender</refname>

    <refpurpose>parse and render WebDyne pages</refpurpose>
  </refnamediv>

  <refsynopsisdiv>
    <cmdsynopsis>
      <command>wdrender [OPTIONS] FILE</command>
    </cmdsynopsis>
  </refsynopsisdiv>

  <refsection>
    <title>Options</title>

    <para><variablelist>
        <varlistentry>
          <term>-h, --help</term>

          <listitem>
            <para>Show brief help message.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>--handler</term>

          <listitem>
            <para>Use a different WebDyne handler module. Currently the only
            other handler module available is
            <classname>WebDyne::Chain</classname>.</para>
          </listitem>
        </varlistentry>
      </variablelist></para>
  </refsection>

  <refsection>
    <title>Description</title>

    <para>The <command>wdrender</command> command displays the HTML that would
    be generated from a psp page using the WebDyne perl module.</para>

    <para>By default <command>wdrender</command> will use the internal WebDyne
    handler when building the output, but can optionally use other WebDyne
    modules (such as <classname>WebDyne::Chain</classname>) by using the
    <option>--handler</option> option..</para>
  </refsection>

  <refsection>
    <title>Notes</title>

    <para>The <command>wdrender</command> command will attempt to build the
    HTML as faithfully as possible from the command line environment, but may
    not be able to exactly duplicate the HTML generated under a real Web
    Server. As an example if a psp page takes advantge of the Apache request
    handler when generating HTML, the <command>wdrender</command> commend will
    not be able to duplicate that environment.</para>
  </refsection>

  <refsection>
    <title>Examples</title>

    <para><command>wdrender widget.psp</command></para>

    <para>Display the HTML that would be generated by WebDyne from the
    widget.psp file.</para>

    <para><command>wdrender --handler=WebDyne::Chain
    widget.psp</command></para>

    <para>Display the HTML that would be generated by WebDyne from the
    widget.psp file, but this time using the
    <classname>WebDyne::Chain</classname> handler. In this case the example
    would fail, as <classname>WebDyne::Chain</classname> expects to get a list
    of modules it will chain the page throught. Normally it would get that
    from the Web Server configuration file, but the
    <classname>WebDyne::Chain</classname> module can take the list of modules
    from the environment also. So a better example would be:</para>

    <para><command>WebDyneChain=WebDyne::Session wdrender
    --handler=WebDyne::Chain widget.psp</command></para>
  </refsection>

  <refsection>
    <title>Author</title>

    <para>Written by Andrew Speer, <email>andrew@webdyne.org</email></para>
  </refsection>

  <refsection>
    <title>Copying</title>

    <para>Copyright © 2008-2010 Andrew Speer. Free use of this software is
    granted under the terms of the GNU General Public License (GPL)</para>
  </refsection>
</refentry>
